{
  "name": "music-school-ots",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check backend/database.mjs && node --check backend/neon-api.mjs && node --check app.js && node --check admin.js && node --check service-worker.js",
    "neon:migrate": "node scripts/migrate-neon.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  },
  "engines": {
    "node": ">=24"
  }
}
